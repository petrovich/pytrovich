[pytest]
testpaths = tests
markers =
    benchmark: performance benchmark using pytest-benchmark; run with `pytest -m benchmark`
# Default: skip benchmarks. Run them explicitly with:
#   pytest -m benchmark
# Or use scripts/benchmark.sh
addopts = -m "not benchmark"
